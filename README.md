# XOR_Enc
Built a lightweight command-line encryption/decryption tool in pure Python that implements the XOR cipher from scratch. The tool takes plaintext and a user-defined key, XORs the data byte-by-byte against a repeating key, and outputs hex-encoded ciphertext — with a matching decryption script to recover the original message.
